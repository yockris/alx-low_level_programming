#include "main.h"
#include <stdio.h>

/**
 * _strlen - will returns the lenght of a string.
 * @s: parameter char pointer
 * Return: (0) success
 */

int _strlen(char *s)
{
	int c;

	for (c = 0; s[c] != 0; c++)
	{
	}
		return (c);

}
